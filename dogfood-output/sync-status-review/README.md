# 同步状态卡片验证

使用实际 SyncStatusCard.vue、进度组件、全局样式和外观设置，内存状态与事件记录，不连接网盘。

- checks.json：3 个宽度 × 3 个字号 × 10 个状态的高度与溢出测量。320px 是取消桌面全局 1000px 最小宽度后的独立容器测试，不代表手机 UI。
- light.png / dark.png：1000px 窗口、18px UI 字号、自定义强调色。
- 验证上传、下载、检查、取消的事件，以及首次预览与确认的区别、历史记录折叠操作。

样式修改没有改变同步协议或实际数据传输。
